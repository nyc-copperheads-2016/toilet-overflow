<% if logged_in? %>
  <%= form_for @answer do |f| %>
    <%= f.hidden_field :user_id, value: current_user.id %>
    <%= f.hidden_field :question_id, value: params[:question_id] %>
    <%= f.text_area :content %>
    <%= f.submit "Add Your Answer" %>
  <% end %>
<% end %>
